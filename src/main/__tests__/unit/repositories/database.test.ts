import { describe, test } from 'vitest'

describe.skip('Database repository (legacy integration suite)', () => {
  test('pending: re-enable with deterministic sqlite integration setup', () => {
    // This suite is intentionally quarantined while sqlite runtime setup is standardized.
  })
})
