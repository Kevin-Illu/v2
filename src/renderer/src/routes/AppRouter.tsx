import { BrowserRouter, Routes } from 'react-router-dom'
import { routes } from './routes'

export const AppRouter = () => {
  return (
    <BrowserRouter>
      {/* <Routes>

      </Routes> */}
    </BrowserRouter>
  )
}
